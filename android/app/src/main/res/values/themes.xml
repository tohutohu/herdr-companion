<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light by default so the window behind Compose (at launch and between
         screens) matches the light colour scheme; values-night overrides it.
         System bars are handled by enableEdgeToEdge(). -->
    <style name="Theme.HerdrMobile" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
